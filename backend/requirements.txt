# Bobcat Advisor — API backend

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Postgres (chat history / feedback / traces)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Retrieval
# fastembed runs all-MiniLM-L6-v2 (and the optional cross-encoder reranker)
# on ONNX Runtime instead of PyTorch — PyTorch's import alone OOM-killed the
# 512MB Render instance. 0.8 adds TextCrossEncoder.
fastembed==0.8.0
chromadb>=0.6.0
numpy>=1.26.0

# LLM — Gemini or Groq via their OpenAI-compatible HTTP APIs (no vendor SDK)
httpx==0.28.1

# Environment variables
python-dotenv==1.0.1

# Knowledge base: text from PDF syllabi (pure Python, no native deps)
pypdf==5.1.0

# Timetable builder: CP-SAT solver. Imported only when a timetable is
# requested (~85MB RSS); TIMETABLE_SOLVER=search avoids it entirely.
ortools==9.15.6755

# Observability: OpenTelemetry traces + metrics over OTLP (app/telemetry.py).
# Versions match what chromadb already pulls in.
opentelemetry-sdk==1.45.0
opentelemetry-exporter-otlp-proto-http==1.45.0
opentelemetry-exporter-otlp-proto-grpc==1.45.0
